#include <iostream> //includes the standard library fot input and output

int main() {
    std::cout << "Hello, World!" << std::endl; 
    // std::cout: prints the output to the console 
    // <<"Hello, World!": send the string "Hello, World!" to std::cout
    //std:endl: ends the line   
    return 0; // ends the main function, signaling that the program completed successfully
}